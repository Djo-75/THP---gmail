<%# <p style="color: green"><%= notice %></p>
<turbo-frame id = "table_frame">

<%= render partial: "email", locals: {message: @email}%>

<div>
  <%= link_to "Editer cet email", edit_user_path(@email) %> |
  <%= link_to "Retour aux emails", emails_path %>

  <%= button_to "Détruire email", @email, method: :delete %>
  <%= button_to "Mail lu/non lu", @email, method: :delete %>
</div>

</turbo-frame>


<turbo-frame id = "table_frame">

<%= render partial: "email", locals: {message: @email}%>

<div>
  <%= link_to "Editer cet email", edit_email_path(@email) %> |
  <%= link_to "Retour aux emails", emails_path %>

  <%= button_to "Delete", @email, method: :delete, data: { turbo_frame: "_top"} %>
  <%= button_to "Mail lu/non lu", @email, data: {turbo_method: :delete} %>
</div>

</turbo-frame>